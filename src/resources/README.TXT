The unicode resource files, with extension "ucd", are created with the Unicode NOTEPAD program from Windows XP (or 2000). They are saved as "Unicode Big Endean".

You can use any unicode editor.

Run convascii.bat to convert them to ASCII.

